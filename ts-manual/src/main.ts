import { _sum } from "./sub";


_sum

const a = 10;

// 타입 어노테이션(주석)
const sum = (a:number, b:number) => a + b;

const b = _sum(1,5);

function greeting(user: String) {
    return `${user}님 반갑습니다.`
}




























































